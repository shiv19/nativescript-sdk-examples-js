<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
	<Page.actionBar>
		<ActionBar title="Cookbook" icon=""/>
	</Page.actionBar>
	<GridLayout>
		<ListView items="{{ links }}" itemTap="{{ onItemTap }}">
			<ListView.itemTemplate>
				<StackLayout>
					<Label text="{{ title }}" />
				</StackLayout>
			</ListView.itemTemplate>
		</ListView>
	</GridLayout>
</Page>
